---
layout: none
---

{
    "plugins": [{% for post in site.posts %}
        {
            "title": "{{ post.title }}",
            "description": "{{ post.description }}",
            "author": "{{ post.author }}",
            "git": "{{ post.git }}"
        },{% endfor %}
    ]
}
